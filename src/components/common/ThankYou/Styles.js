import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  button: {
    borderRadius: 30,
    borderWidth: 1,
    borderColor: '#444',
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 15,
    textAlign: 'center',
    width: 250,
    height: 'auto',
    backgroundColor: '#0808fdad',
  },
  btntext: {
    fontSize: 18,
    color: '#fff',
    padding: 8,
  },
  text: {
    fontSize: 20,
    color: '#999999d2',
  },
});

export default styles;
